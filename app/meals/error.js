'use client'

export default function Error ({error}) {
    return <main className="error">
        <h1>Ops! cadê?</h1>
        <p>Falhamos ao tentar carregar as refeições. Tente novamente mais tarde.</p>

    </main>
}